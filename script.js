const menu=document.querySelector('.menu-toggle'),nav=document.querySelector('.nav');menu.addEventListener('click',()=>nav.classList.toggle('open'));document.querySelectorAll('.nav a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));
const player=document.getElementById('player'),title=document.getElementById('playerTitle');document.querySelectorAll('.play').forEach(b=>b.addEventListener('click',()=>{title.textContent=b.dataset.title;player.classList.add('show')}));document.getElementById('stop').addEventListener('click',()=>player.classList.remove('show'));document.getElementById('year').textContent=new Date().getFullYear();
document.getElementById('contactForm').addEventListener('submit',e=>{e.preventDefault();document.getElementById('formMessage').textContent='Thanks! For now, please use the WhatsApp or email links above to contact Lathamix.'});
